{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rustchap.dev/schemas/pack-index.schema.json",
  "title": "RustChap pack index",
  "description": "Curriculum order of the packs — the single source of truth for track sequence, read by the API (and eventually clients). Lives at content/packs/index.json.",
  "type": "object",
  "required": ["schema_version", "order"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "const": 1 },
    "order": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "pattern": "^[a-z0-9-]+$" }
    }
  }
}
