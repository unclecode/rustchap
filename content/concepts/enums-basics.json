{
  "schema_version": 1,
  "id": "enums-basics",
  "title": "Enums",
  "topic": "Data shapes",
  "summary": "An enum is a type whose value is exactly one of a few named variants.",
  "lecture": [
    "An enum declares a type whose value is exactly one of a few named variants. Most languages have a version of this as a list of constants.",
    "Rust goes further. A variant can carry data, and each variant can carry something different. `Option` and `Result` are ordinary enums built this way, which is why they feel so natural in Rust.",
    "You read an enum back with `match`, which tells you which variant you have and binds whatever it carries. The match must cover every variant, and missing one is error `E0004`. That check is what turns a new variant into a compile error rather than a bug found in production."
  ],
  "example": {
    "code": "enum Command {\n    Add(i32),\n    Reset,\n}\n\nmatch cmd {\n    Command::Add(n) => total + n,\n    Command::Reset => 0,\n}",
    "caption": "n binds to the number Add is carrying."
  }
}
