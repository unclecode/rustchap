{
  "id": "option.gotcha.no-null",
  "kind": "gotcha",
  "title": "Absence as an explicit type",
  "concepts": [
    "option"
  ],
  "prompt": "Why does Rust use Option instead of null pointers for missing values?",
  "answer": "Null allows missing values to be passed silently until they cause a runtime crash. `Option<T>` forces absence into the type system so the compiler requires every caller to handle the `None` case explicitly.",
  "schema_version": 1
}
