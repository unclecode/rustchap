{
  "schema_version": 1,
  "id": "bindings.rule.default",
  "kind": "rule",
  "title": "Mutable or not by default",
  "concepts": [
    "bindings"
  ],
  "prompt": "What is the default mutability of a `let` binding, and how do you change it?",
  "answer": "Immutable. `let x = 5;` binds the name once, and assigning to it again is a compile error, not a warning. You opt into change with `let mut x = 5;`.",
  "example": {
    "code": "let x = 5;\nx = 6;        // error E0384",
    "caption": "Assigning twice to an immutable binding."
  }
}
